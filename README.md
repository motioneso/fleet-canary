# fleet-canary

A throwaway repository used once to prove the Fleet engine works against real
GitHub, real agent tools and a real terminal multiplexer. Nothing here is meant
to be useful on its own.

Run the tests with `go test ./...`.
